small repo to move files to the hpc without messing with ssh every time

- opt.py - file to optimise stardist paramaters
- sdist.py - file to train a model with stardist
- util.py - useful functions (merging channels, checking tif similarity)

`-m` files are for multi channel
